# 🚀 راهنمای دیپلوی روی لیارا

این راهنما نحوه دیپلوی ربات تریدینگ سیگنال را روی پلتفرم لیارا با لوکیشن آلمان توضیح می‌دهد.

---

## 📋 پیش‌نیازها

1. **حساب لیارا**: ثبت‌نام در [liara.ir](https://liara.ir)
2. **Git**: برای آپلود کد
3. **Token تلگرام**: از [@BotFather](https://t.me/BotFather) دریافت کنید

---

## ⚙️ تنظیمات لیارا

### 1. ایجاد اپلیکیشن در لیارا

1. وارد داشبورد لیارا شوید
2. روی "Create New App" کلیک کنید
3. نام اپلیکیشن را وارد کنید: `wewewewe` (یا نام دلخواه)
4. نوع اپلیکیشن: **Worker**
5. لوکیشن: **Germany** (آلمان)

### 2. تنظیم متغیرهای محیطی

در داشبورد لیارا، به بخش **Environment Variables** بروید و متغیرهای زیر را اضافه کنید:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
INITIAL_BALANCE=20.0
MAX_RISK_PERCENT=2.0
MODEL_CONFIDENCE_THRESHOLD=0.75
EXCHANGE_NAME=binance
USE_REAL_DATA=true
```

**مهم**: 
- `TELEGRAM_BOT_TOKEN` را از [@BotFather](https://t.me/BotFather) دریافت کنید
- حتماً `USE_REAL_DATA=true` را تنظیم کنید

### 3. آپلود کد

#### روش 1: از طریق Git (پیشنهادی)

```bash
# اگر پروژه Git ندارد
git init
git add .
git commit -m "Initial commit"

# اضافه کردن remote لیارا
git remote add liara git@liara.ir:YOUR_USERNAME/wewewewe.git

# آپلود کد
git push liara main
```

#### روش 2: از طریق CLI لیارا

```bash
# نصب CLI لیارا
npm install -g @liara/cli

# لاگین به لیارا
liara login

# دیپلوی
liara deploy --app wewewewe
```

---

## 📁 فایل‌های مهم برای لیارا

### `liara.json`
فایل پیکربندی لیارا که لوکیشن را روی آلمان تنظیم می‌کند:

```json
{
  "app": "wewewewe",
  "port": 80,
  "build": {
    "location": "germany"
  },
  "disks": []
}
```

### `Procfile`
تعریف فرآیند Worker:

```
worker: python main.py
```

### `requirements.txt`
همان فایل موجود که شامل تمام dependencies است.

---

## 🔍 بررسی لاگ‌ها

بعد از دیپلوی، لاگ‌ها را بررسی کنید:

```bash
# از طریق CLI
liara logs --app wewewewe

# یا از داشبورد لیارا
# بخش Logs
```

---

## ✅ چک لیست بعد از دیپلوی

- [ ] اپلیکیشن روی لیارا با لوکیشن آلمان راه‌اندازی شده
- [ ] تمام متغیرهای محیطی تنظیم شده‌اند
- [ ] ربات در حال اجراست (Status: Running)
- [ ] لاگ‌ها نشان می‌دهد "SYSTEM READY"
- [ ] می‌توانید با `/start` در تلگرام ربات را تست کنید

---

## 🐛 عیب‌یابی

### مشکل: ربات راه‌اندازی نمی‌شود

1. **بررسی لاگ‌ها**: `liara logs --app wewewewe`
2. **بررسی متغیرها**: مطمئن شوید `TELEGRAM_BOT_TOKEN` تنظیم شده
3. **بررسی Port**: لیارا به صورت خودکار Port را مدیریت می‌کند

### مشکل: ربات به داده‌های واقعی دسترسی ندارد

1. مطمئن شوید `USE_REAL_DATA=true` است
2. بررسی کنید API Binance در آلمان قابل دسترسی است
3. بررسی لاگ‌ها برای خطاهای شبکه

### مشکل: لوکیشن آلمان کار نمی‌کند

1. در `liara.json` مطمئن شوید `"location": "germany"` است
2. در داشبورد لیارا، Region را بررسی کنید
3. بعد از تغییر، دوباره دیپلوی کنید

---

## 🔄 بروزرسانی ربات

بعد از هر تغییر در کد:

```bash
git add .
git commit -m "Update bot"
git push liara main
```

لیارا به صورت خودکار build و deploy می‌کند.

---

## 📊 مانیتورینگ

- **Logs**: از داشبورد لیارا یا CLI
- **Metrics**: CPU, Memory, Network از داشبورد
- **Uptime**: بررسی Status در داشبورد

---

## 💰 هزینه‌ها

- Worker: معمولاً رایگان تا حد مشخصی
- لوکیشن آلمان: ممکن است هزینه‌ای اضافه داشته باشد
- Bandwidth: برای API calls به Binance

---

## 🎯 مزایای لوکیشن آلمان

1. **دسترسی به تلگرام**: بدون فیلتر
2. **دسترسی به Binance API**: بدون محدودیت
3. **سرعت بهتر**: برای API calls
4. **قابلیت اطمینان**: زیرساخت بهتر

---

## 📝 نکات مهم

1. **هرگز Token را در کد commit نکنید**: از Environment Variables استفاده کنید
2. **Database**: SQLite به صورت local storage ذخیره می‌شود (موقت است)
3. **Restart**: بعد از تغییر متغیرهای محیطی، ربات را restart کنید
4. **Backup**: برای داده‌های مهم، backup بگیرید

---

## 🆘 پشتیبانی

- **مستندات لیارا**: [docs.liara.ir](https://docs.liara.ir)
- **لاگ‌های خطا**: از داشبورد لیارا بررسی کنید
- **تلگرام Bot**: با `/help` دستورات را ببینید

---

## ✅ تست نهایی

بعد از دیپلوی، این دستورات را در تلگرام تست کنید:

```
/start       # شروع ربات
/signal      # تولید سیگنال
/stats       # آمار معاملات
/balance     # موجودی حساب
/help        # راهنما
```

اگر تمام دستورات کار کردند، دیپلوی موفق بوده است! 🎉

