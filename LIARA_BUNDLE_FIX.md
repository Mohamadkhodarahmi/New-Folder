# 🔧 حل مشکل Bundle Plan در لیارا

## ⚠️ مشکل

```
"The germany builder is not allowed for your bundle plan."
```

پلن Bundle شما لوکیشن آلمان را پشتیبانی نمی‌کند.

---

## ✅ راه‌حل‌ها

### راه‌حل 1: استفاده از لوکیشن پیش‌فرض (پیشنهادی)

فایل `liara.json` را به این صورت تنظیم کنید:

```json
{
  "app": "wewewewe",
  "port": 80,
  "build": {},
  "disks": []
}
```

**نکته**: با حذف `location`، لیارا از لوکیشن پیش‌فرض (معمولاً ایران) استفاده می‌کند.

### راه‌حل 2: استفاده از Proxy برای تلگرام

اگر در ایران هستید و تلگرام فیلتر است، می‌توانید از Proxy استفاده کنید:

#### تنظیم Proxy در Environment Variables

در داشبورد لیارا → Environment Variables → اضافه کنید:

```
TELEGRAM_PROXY=socks5://username:password@proxy-host:port
```

یا برای HTTP proxy:

```
TELEGRAM_PROXY=http://proxy-host:port
```

**مثال‌های Proxy رایگان:**
- SOCKS5: `socks5://user:pass@example.com:1080`
- HTTP: `http://proxy.example.com:8080`

#### Proxy های رایگان برای تست:

⚠️ **هشدار**: Proxy های رایگان ممکن است ناپایدار باشند. برای استفاده تولیدی از سرویس‌های معتبر استفاده کنید.

---

## 🚀 دیپلوی بعد از تغییر

بعد از تغییر `liara.json`:

```bash
git add liara.json
git commit -m "Fix location for bundle plan"
git push liara main
```

---

## 📋 گزینه‌های دیگر

### 1. ارتقای پلن

اگر نیاز به لوکیشن آلمان دارید:
- وارد داشبورد لیارا شوید
- پلن خود را ارتقا دهید
- بعد از ارتقا می‌توانید `"location": "germany"` را دوباره اضافه کنید

### 2. استفاده از VPS

اگر نیاز به لوکیشن خاص دارید:
- از VPS در آلمان استفاده کنید (Hetzner, DigitalOcean, etc.)
- کد را روی VPS دیپلوی کنید

---

## ✅ وضعیت فعلی

فایل `liara.json` به‌روزرسانی شده و:
- ✅ `location` حذف شده (استفاده از پیش‌فرض)
- ✅ پشتیبانی از Proxy اضافه شده
- ✅ آماده دیپلوی روی Bundle Plan

---

## 🧪 تست بعد از دیپلوی

1. **بدون Proxy**: اگر در ایران هستید و تلگرام فیلتر است، از Proxy استفاده کنید
2. **با Proxy**: مطمئن شوید Proxy در Environment Variables تنظیم شده
3. **تست ربات**: `/start` را در تلگرام بفرستید

---

## 📝 نکات مهم

1. **لوکیشن پیش‌فرض**: معمولاً ایران است (برای Bundle Plan)
2. **Proxy**: برای دسترسی به تلگرام در ایران ضروری است
3. **Binance API**: معمولاً از ایران قابل دسترسی است
4. **ارتقای پلن**: اگر نیاز به لوکیشن خاص دارید، پلن را ارتقا دهید

---

## 🆘 مشکل حل نشد؟

1. لاگ‌ها را بررسی کنید: `liara logs --app wewewewe`
2. Environment Variables را بررسی کنید
3. با پشتیبانی لیارا تماس بگیرید

